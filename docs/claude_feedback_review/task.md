# タスクリスト

- [x] `backend_calc.py`の列名マッチング修正（改行除去）
- [x] `backend_calc.py`の不要変数にコメント追加（Kanken需要予測）
- [x] `dashboard.html`への期首在庫に関する注記追加（店舗在庫を含む旨）
- [x] `backend_calc.py`の月数カウント修正（`VALID_MONTHS`利用）
- [x] `backend_calc.py`のゼロ行除外処理追加（A案：バックエンドでの除外）
- [x] `backend_calc.py`の進捗率定数にコメント追加（2026年適用前提）
- [x] `backend_calc.py`を実行し、出力ファイル（`検証用_統合データ.csv`, `dashboard_data.js`）が正常に生成されるか確認
