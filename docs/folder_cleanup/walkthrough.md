# フォルダ整理の完了報告書（Folder Cleanup Walkthrough）

フォルダ内の整理整頓が完了しました。作業用の一時ファイルをすべて退避フォルダに移動し、本番の運用に必要なファイルだけがルートに残る状態にしました。

## 実施した内容

1. **バックアップ用フォルダの作成**
   - ルートフォルダ直下に `work_backup` フォルダを新規作成しました。
2. **一時ファイルの退避（計30ファイル）**
   - **検証用スクリプト (11件)**: `check_csv.py`, `check_v6.py`, `finalize_v6.py` など
   - **調査・ログ用テキスト (17件)**: `v5_logic.txt`, `jisseki_check.txt` など
   - **一時データおよびロックファイル (2件)**: `検証用_統合データ.csv`, `.~lock.Kanken23510_2026年版_v6.xlsx#`
3. **本番動作確認**
   - 整理後に `データ更新を実行する.bat` を実行し、データ更新プログラム (`backend_calc.py`) が問題なく動作すること、ダッシュボード用データ (`dashboard_data.js`) が正常に更新されることを確認しました。

---

## 整理後のルートフォルダ構成

現在、以下の必要最小限のファイルだけが並んでおり、非常に見通しの良い状態になっています。

- **シミュレーション・実績データ**:
  - `Kanken23510_2026年版_v6.xlsx`（最新のシミュレーション）
  - `Kanken23510_2026年版_v5.xlsx`（前バージョンのシミュレーション）
  - `2026実績.csv`（最新の実績データ）
  - `6.24までの実績.xlsx`（元の実績ファイル）
  - `分配店舗向け.xlsx` / `分配店舗向けclaude用.xlsx`（元の配布ファイル）
- **ダッシュボードシステム**:
  - `dashboard.html`（可視化ダッシュボード画面）
  - `dashboard_data.js`（ダッシュボード用データ）
  - `dashboard_data.json`（ダッシュボード用データバックアップ）
  - `backend_calc.py`（データ集計プログラム）
  - `データ更新を実行する.bat`（データ更新を実行するバッチ）
- **退避・記録用フォルダ**:
  - `docs/`（ドキュメント用フォルダ）
  - `work_backup/`（今回一時ファイルを移動したフォルダ）
